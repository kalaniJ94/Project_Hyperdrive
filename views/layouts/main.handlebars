<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Project Hyperdrive</title>
    <link href="/css/output.css" rel="stylesheet" />
    <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Handjet:wght@300&family=Pixelify+Sans&family=Shadows+Into+Light&display=swap" rel="stylesheet">
</head>

<body>
    <header id="header">
      {{#if logged_in}}
    <div>
    <a id="logout-icon" href="#" class="logout-btn">Logout</a>
    </div>
      {{/if}}


</header>
    <main>
        {{{body}}}
    </main>
    {{!-- add js script  --}}
    {{#if logged_in}}
    <script src="/js/logout.js"></script>
    {{/if}}
</body>

</html>